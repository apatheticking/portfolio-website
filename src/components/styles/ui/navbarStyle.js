import { makeStyles } from '@material-ui/core/styles'

export const navbarStyle = makeStyles({
    root: {
        background: '#30342F',
        '& a':{
            color: 'white',
            textDecoration: 'none'
        }
    }
});